- id: commit-issue-prefix
  name: commit-issue-prefix
  description: Prefix the commit message with the issue token (and optional type) taken from the branch name
  entry: commit-issue-prefix.sh
  language: script
  stages: [prepare-commit-msg]
  always_run: true
